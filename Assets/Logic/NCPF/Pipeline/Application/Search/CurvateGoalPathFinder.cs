namespace NCPF.Pipeline.Application
{
    /// <summary>Finder over the GEOMETRIC 3D lattice. Same algorithm as
    /// <see cref="GoalPathFinderBase{T}"/> — only the edge type differs.</summary>
    public class CurvateGoalPathFinder : GoalPathFinderBase<CurvateTransition>
    {
    }
}
